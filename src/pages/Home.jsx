import DonateCard from '../components/DonateCard';

const Home = () => {
  return (
    <div className="min-h-screen flex items-center justify-center p-4 bg-[url('/canal-lines.svg')] bg-cover bg-center bg-no-repeat bg-white">
      <DonateCard />
    </div>
  );
};

export default Home;
